import VideoCallFull from "./videoCallFull";

const VideoSidebar = () => {
  return (
    <section className="fixed inset-0 bg-black/80 z-50 flex items-center justify-center">
      <VideoCallFull />
    </section>
  );
};

export default VideoSidebar;
